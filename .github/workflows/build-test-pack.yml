name: build-test-pack

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore ISOCodex.Addressing.sln

      - name: Build
        run: dotnet build ISOCodex.Addressing.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test ISOCodex.Addressing.sln --configuration Release --no-build

      - name: Pack core package
        run: dotnet pack src/Addressing/Addressing.csproj --configuration Release --no-build -o ./artifacts

      - name: Pack Great Britain package
        run: dotnet pack src/Addressing.GreatBritain/Addressing.GreatBritain.csproj --configuration Release --no-build -o ./artifacts

      - name: Pack United States package
        run: dotnet pack src/Addressing.UnitedStates/Addressing.UnitedStates.csproj --configuration Release --no-build -o ./artifacts

      - name: Pack Canada package
        run: dotnet pack src/Addressing.Canada/Addressing.Canada.csproj --configuration Release --no-build -o ./artifacts

      - name: Pack France package
        run: dotnet pack src/Addressing.France/Addressing.France.csproj --configuration Release --no-build -o ./artifacts

      - name: Pack Ireland package
        run: dotnet pack src/Addressing.Ireland/Addressing.Ireland.csproj --configuration Release --no-build -o ./artifacts

      - name: Pack Spain package
        run: dotnet pack src/Addressing.Spain/Addressing.Spain.csproj --configuration Release --no-build -o ./artifacts

      - name: Upload packages
        uses: actions/upload-artifact@v4
        with:
          name: nupkg
          path: ./artifacts/*.nupkg
